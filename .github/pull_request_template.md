## What changes

<!-- What the change does, and why. Link the issue if there is one. -->

## How it was checked

<!--
Which of these you ran, and what came back. Not every change needs all of
them — a README fix needs none.

  npm run lint
  npm run typecheck
  npm test
  npm run package

For a provider change, the useful evidence is the completion it now returns
for a given template, not that the suite is green.
-->
